pipeline {
    tools {
        nodejs 'nodejs'
    }
    agent any

    stages {
        stage('Building the project...') {
            steps {
                sh 'npm install'
            }
        }
    }
}
